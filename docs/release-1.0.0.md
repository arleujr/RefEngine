# RefEngine 1.0.0

This is the first public source release of RefEngine. Internal prototype version numbers were intentionally reset so repository history can evolve from a clear public baseline.

The source repository does not include `frontend/dist`, virtual environments, Node modules, private documents, local databases, or generated output.
