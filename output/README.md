# Output

`output/latest/` is written only after a run passes the quality gate and is published from the React interface.

Final files:

- `references_ufv.docx`;
- `references_ufv.txt`;
- `publication.json`.

Drafts, immutable input snapshots, evidence, and review state are stored locally under `data/runs/{run_id}/`.
